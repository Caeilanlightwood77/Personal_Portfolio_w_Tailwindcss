import Typed from 'typed.js';

document.addEventListener('DOMContentLoaded', function() {
  var options = {
    strings: ["Back-End Developer", "Software Developer", "Data Analyst", "Cryptanalyst"],
    typeSpeed: 50,
    backSpeed: 25,
    backDelay: 1000,
    startDelay: 500,
    loop: true
  };

  var typed = new Typed('.typed', options);
});
